sensor_reading=float(input("Enter the reading ="))
lower_limit=int(input("Enter the lower limit ="))
upper_limit=int(input("Enter the upper limit ="))
if sensor_reading >= lower_limit and sensor_reading <= upper_limit:
   print("SAFE")
else:
   print("UNSAFE")
